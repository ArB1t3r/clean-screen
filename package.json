{
  "name": "clean-screen",
  "title": "Clean Screen",
  "description": "Turn your display into a dark cleaning screen and block keyboard input until you exit.",
  "author": "Eigenlicht",
  "license": "MIT",
  "icon": "icon.png",
  "platforms": [
    "macOS"
  ],
  "categories": [
    "System"
  ],
  "commands": [
    {
      "name": "index",
      "title": "Start Cleaning Session",
      "description": "Show a black full-screen cleaning overlay and block keyboard input",
      "mode": "no-view",
      "icon": "icon.png",
      "keywords": [
        "screen",
        "clean",
        "keyboard",
        "overlay",
        "display"
      ]
    }
  ],
  "scripts": {
    "dev": "npx ray develop",
    "build": "npx ray build",
    "lint": "npx ray lint",
    "publish": "npx @raycast/api@latest publish",
    "build:helper": "zsh ./scripts/build-helper.sh"
  },
  "dependencies": {
    "@raycast/api": "^1.103.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
